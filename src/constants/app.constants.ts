export enum Role {
  SuperAdmin = 'SUPER_ADMIN',
  Admin = 'ADMIN',
  Supervisor = 'SUPERVISOR',
}

export enum APP_ENVIRONMENT {
  PRODUCTION = 'production',
  STAGING = 'staging',
  DEVELOPMENT = 'development',
}

export enum APP_TIMEZONE {
  UTC = 'UTC',
  AFRICA_CAIRO = 'Africa/Cairo',
}

export enum APP_LANGUAGE {
  EN = 'en',
  AR = 'ar',
}

export const IS_PUBLIC_KEY = 'IS_PUBLIC_KEY';
export const SKIP_MAINTENANCE_DECORATOR_KEY = 'SKIP_MAINTENANCE_METADATA_KEY';
